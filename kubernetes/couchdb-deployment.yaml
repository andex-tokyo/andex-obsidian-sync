# kubernetes/couchdb-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: couchdb
  namespace: obsidian-sync
spec:
  replicas: 1
  selector:
    matchLabels:
      app: couchdb
  template:
    metadata:
      labels:
        app: couchdb
    spec:
      initContainers:
        - name: setup-config
          image: couchdb:3.5.0
          command:
            - /bin/sh
            - -c
            - |
              echo "Setting up persistent configuration..."

              cat > /persistent-config/obsidian.ini << 'EOF'
              [couchdb]
              single_node=true
              max_document_size = 50000000

              [chttpd]
              require_valid_user = true
              max_http_request_size = 4294967296
              bind_address = 0.0.0.0
              port = 5984

              [chttpd_auth]
              require_valid_user = true

              [httpd]
              WWW-Authenticate = Basic realm="couchdb"
              enable_cors = true
              bind_address = 0.0.0.0
              port = 5986

              [cors]
              origins = *
              credentials = true
              methods = GET, PUT, POST, HEAD, DELETE
              headers = accept, authorization, content-type, origin, referer, x-requested-with
              EOF

              echo "Configuration created at /persistent-config/obsidian.ini:"
              cat /persistent-config/obsidian.ini

              # 権限設定
              chown couchdb:couchdb /persistent-config/obsidian.ini || true
              chmod 644 /persistent-config/obsidian.ini || true

          volumeMounts:
            - name: couchdb-config-persistent
              mountPath: /persistent-config

      containers:
        - name: couchdb
          image: couchdb:3.5.0
          env:
            - name: COUCHDB_USER
              value: "admin"
            - name: COUCHDB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: couchdb-secrets
                  key: admin-password
            - name: COUCHDB_SECRET
              valueFrom:
                secretKeyRef:
                  name: couchdb-secrets
                  key: cookie

          ports:
            - containerPort: 5984
              name: couchdb

          volumeMounts:
            - name: couchdb-data
              mountPath: /opt/couchdb/data
            - name: couchdb-config-persistent
              mountPath: /opt/couchdb/etc/local.d

          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "500m"

          readinessProbe:
            httpGet:
              path: /_up
              port: 5984
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 10

      volumes:
        - name: couchdb-data
          persistentVolumeClaim:
            claimName: couchdb-data-pvc
        - name: couchdb-config-persistent
          persistentVolumeClaim:
            claimName: couchdb-config-pvc

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: couchdb-data-pvc
  namespace: obsidian-sync
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: couchdb-config-pvc
  namespace: obsidian-sync
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
